<div class='container'>
  <h3>管理者Log in</h3>
  <div class='row'>
    <div class="col-sm-30 col-lg-5 px-sm-0 mx-auto">

      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="form-group row mb-3">
          <%= f.label :email, "メールアドレス", class: "col-sm-5 col-form-label" %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "sample@example.com", size: 30 %>
        </div>

        <div class="form-group row mb-3">
          <%= f.label :password, "パスワード", class: "col-sm-5 col-form-label" %><br />
          <%= f.password_field :password, autocomplete: "current-password", size: 30 %>
        </div>

        <!--<% if devise_mapping.rememberable? %>-->
        <!--  <div class="field">-->
        <!--    <%= f.check_box :remember_me %>-->
        <!--    <%= f.label :remember_me %>-->
        <!--  </div>-->
        <!--<% end %>-->

        <div class="form-group text-center">
          <%= f.submit "ログイン", class: "btn btn-primary"%>
        </div>
      <% end %>

<!--<%= render "devise/shared/links" %>-->

    </div>
  </div>
</div>